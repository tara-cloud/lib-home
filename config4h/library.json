{
  "name": "config4h",
  "version": "1.0.0",
  "description": "Device configuration over MQTT for ESP32 — single JSON document, dot-path access, live change callbacks",
  "keywords": ["config", "mqtt", "esp32", "json", "iot"],
  "frameworks": ["arduino"],
  "platforms": ["espressif32"],
  "dependencies": {
    "bblanchon/ArduinoJson": "^7.0.0",
    "knolleary/PubSubClient": "^2.8"
  }
}
